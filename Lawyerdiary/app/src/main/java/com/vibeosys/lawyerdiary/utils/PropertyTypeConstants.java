package com.vibeosys.lawyerdiary.utils;

/**
 * Created by Vibeosys software on 11-10-2016.
 */

/**
 * Property constants are the keys that is use to store the shared preference data
 * in key value pair
 */
public class PropertyTypeConstants {
    public static final String API_ENDPOINT_URI = "api.endpoint.uri";
    // Store the user id with user.id key
    public static final String USER_ID = "user.id";
    // Store the name with user.name key
    public static final String USER_NAME = "user.name";
    // Store the email with user.email key
    public static final String USER_EMAIL_ID = "user.email";
    // Store the password with user.password key
    public static final String USER_PASSWORD = "user.password";
    // Store the purchase with user.purchase key
    public static final String USER_PURCHASE = "user.purchase";
}
